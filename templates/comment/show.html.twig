{% extends 'baseback.html.twig' %}

{% block title %}Listes des Commentaire{% endblock %}

{% block content1 %}
  <center>
    <h1>listes des Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>contenu</th>
                 <td>{{ comment.description }}</td>
             </tr>
            <tr>
                <th>Note</th>
                <td>{{ comment.rate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comment_index') }}">Retour</a>

    <a href="{{ path('app_comment_edit', {'id': comment.id}) }}">Modifier</a>

    {{ include('comment/_delete_form.html.twig') }}
    </center>
{% endblock %}
