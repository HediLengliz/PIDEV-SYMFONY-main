<form method="post" action="{{ path('app_article_delete', {'title': article.title}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input  type="hidden" name="_token" value="{{ csrf_token('delete' ~ article.title) }}">
    <button class="btn-danger btn-sm btn-info">Delete</button>
</form>
