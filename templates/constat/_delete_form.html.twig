<form method="post" action="{{ path('app_constat_delete', {'id': sinister_vehicle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sinister_vehicle.id) }}">
    <button class="btn btn-danger mt3">Delete</button>
</form>
