<form method="post" action="{{ path('app_remorqueur_delete', {'id': remorqueur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ remorqueur.id) }}">
    <button class="btn btn-danger mt3">Delete</button>
</form>
