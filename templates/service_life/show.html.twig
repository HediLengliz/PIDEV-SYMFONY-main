{% extends 'admin/index.html.twig' %}

{% block title %}ServiceLife{% endblock %}

{% block profile %}
    <h1>ServiceLife</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service_life.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ service_life.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ service_life.price }}</td>
            </tr>
        </tbody>
    </table>

<a class="btn btn-secondary" href="{{ path('app_question_show',{'id': id}) }}">Back to List</a>

    <a class="btn btn-info" href="{{ path('app_service_life_edit', {'id': service_life.id}) }}">edit</a>

    {{ include('service_life/_delete_form.html.twig') }}
{% endblock %}
