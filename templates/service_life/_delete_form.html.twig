<form method="post" action="{{ path('app_service_life_delete', {'id': service_life.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service_life.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
