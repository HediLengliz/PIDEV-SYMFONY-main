{% extends 'base.html.twig' %}

{% block title %}PropretyRequest{% endblock %}

{% block body %}
<div class="section-title">
         <h2>Demande habitat {{ proprety_request.id }}</h2>
      </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proprety_request.id }}</td>
            </tr>
            <tr>
                <th>DateRequest</th>
                <td>{{ proprety_request.dateRequest ? proprety_request.dateRequest|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>TypeInsurance</th>
                <td>{{ proprety_request.typeInsurance }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ proprety_request.status }}</td>
            </tr>
            <tr>
                <th>PropertyForme</th>
                <td>{{ proprety_request.PropertyForme }}</td>
            </tr>
            <tr>
                <th>NumberRooms</th>
                <td>{{ proprety_request.NumberRooms }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ proprety_request.Address }}</td>
            </tr>
            <tr>
                <th>PropertyValue</th>
                <td>{{ proprety_request.PropertyValue }}</td>
            </tr>
            <tr>
                <th>Surface</th>
                <td>{{ proprety_request.Surface }}</td>
            </tr>
        </tbody>
    </table>
    <div class="col-12">
        <div class="row">
            <div class="col">
                <a href="{{ path('app_proprety_request_index') }}" class="btn btn-primary w-100">back to list</a>
            </div>
            <div class="col">
                <a href="{{ path('app_proprety_request_edit', {'id': proprety_request.id}) }}" class="btn btn-primary w-100">edit</a>
            </div>
        </div>
    </div>



    {{ include('proprety_request/_delete_form.html.twig') }}
{% endblock %}
